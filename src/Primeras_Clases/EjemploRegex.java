package Primeras_Clases;

import java.util.regex.*; 

public class EjemploRegex {

	public static void main(String[] args) {
		
		 String correo = "gguillermo1990@gmail.com";
         
	     // Definir el patrón de una dirección de correo 
	     //String patron = "^[A-Za-z0-9+.-_]+@[A-Za-z0-9.-]+$"; 
	     //String patron2 = "guille";
	     //String patron3 = "gguillermo";
	         
	     // Compilar la expresión regular en un objeto Pattern 
	     //Pattern pattern = Pattern.compile(patron); 
	     //Pattern pattern2 = Pattern.compile(patron2);
	     //Pattern pattern3 = Pattern.compile(patron3);
	         
	     // Crear un Matcher para verificar si la cadena coincide 
	     //Matcher matcher = pattern.matcher(correo); 
	     //Matcher matcher2 = pattern2.matcher(correo);
	     //Matcher matcher3 = pattern3.matcher(correo);
	     
	         
//	     if (matcher.matches()) { 
//	         System.out.println("El correo es válido."); 
//	     } else { 
//	         System.out.println("El correo no es válido."); 
//	     }
	     
//	     if (matcher2.find()) { 
//	         System.out.println("El correo contiene la palabra: " + patron2); 
//	     } else { 
//	         System.out.println("El correo NO contiene la palabra: " + patron2); 
//	     }
	     
//	     String nuevaCadena = matcher3.replaceAll("ggomez");
//	     System.out.println(nuevaCadena);
	}
}
